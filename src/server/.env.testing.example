# テスト時に必要かつ不変な値のみを定義する
# 変化しうるアプリ設定は各テストで都度設定する
# ATLAS_DB_PORT のみ worktree:init が worktree ごとの MySQL 公開ポートへ上書きする

DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
ATLAS_DB_PORT=3306
DB_DATABASE=isekai_observatory_testing
DB_USERNAME=app
DB_PASSWORD=secret
DB_ROOT_PASSWORD=root
